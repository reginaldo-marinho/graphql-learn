# import Usuario from 'usuario.graphql'
type Mutation {
    novoUsuario(nome: String email: String, idade: Int) : Usuario!
    excluirUsuario(id: ID):Usuario
    alterarUsuario(id: ID, nome:String, email: String, idade: Int):Usuario!
}