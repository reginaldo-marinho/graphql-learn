# import Usuario, InputUsuario, FiltroUpdateUsuario from 'usuario.graphql'
type Mutation {
    novoUsuario(input: InputUsuario) : Usuario!
    excluirUsuario(input: InputUsuario):Usuario
    alterarUsuario(filtro: FiltroUpdateUsuario, input: InputUsuario):Usuario!
}